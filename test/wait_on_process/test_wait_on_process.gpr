with
     "../../ashell",
     "../../shared";

project Test_Wait_On_Process
is
   for Object_Dir use "build";
   for Exec_Dir   use ".";
   for Main       use ("test_wait_on_process.adb");

   package Compiler renames Shared.Compiler;

end Test_Wait_On_Process;
