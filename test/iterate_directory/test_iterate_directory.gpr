with
     "../../ashell",
     "../../ashell_shared";

project Test_Iterate_Directory 
is
   for Object_Dir use "build";
   for Exec_Dir   use ".";
   for Main       use ("test_iterate_directory.adb");

   package Compiler renames aShell_shared.Compiler;

end Test_Iterate_Directory;
