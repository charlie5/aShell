with "../../ashell";

project Default is

   for Object_Dir use "build";
   for   Exec_Dir use ".";

   for Main use ("environment_test_runner.adb");

   package Compiler renames Ashell.Compiler;

end Default;
