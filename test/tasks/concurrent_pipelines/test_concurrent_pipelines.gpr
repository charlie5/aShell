with
     "../../../ashell",
     "../../../shared";

project Test_Concurrent_Pipelines
is
   for Object_Dir use "build";
   for Exec_Dir   use ".";
   for Main       use ("test_concurrent_pipelines.adb");

   package Compiler renames Shared.Compiler;

end Test_Concurrent_Pipelines;
