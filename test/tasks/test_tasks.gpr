with
     "../../ashell",
     "../../shared";

project Test_Tasks
is
   for Object_Dir use "build";
   for Exec_Dir   use ".";
   for Main       use ("test_tasks.adb");

   package Compiler renames Shared.Compiler;

end Test_Tasks;
