with
     "ashell",
     "ashell_shared";

project Test_Pause_Resume
is
   for Object_Dir use "build";
   for Exec_Dir   use ".";
   for Main       use ("test_pause_resume.adb");

   package Compiler renames aShell_shared.Compiler;

end Test_Pause_Resume;
